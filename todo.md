# 待办清单

## 2026-06-18 解析历史详情“弹窗 -> 完整页面”

### 本次目标
1. 将解析历史详情从弹窗迁移为独立路由页面，在主区域展示
2. 修复并更新前端测试用例，保证 CI checks 全绿

### 执行计划
1. 前端路由与入口校验
   - 确认 `"frontend/src/App.tsx"` 已存在 `/video/analysis/history/:analysisId` 路由
   - 确认历史列表点击“查看 / 打标”跳转路由（不再打开弹窗）
2. 页面组件补齐（以 `"frontend/src/pages/video-analysis/VideoAnalysisHistoryDetail.tsx"` 为主）
   - 基本信息区块、解析结果详情展示、标签区块（当前标签 + 解析结果打标面板）
   - 返回入口（返回“视频解析”列表）
3. 测试修复
   - 更新 `"frontend/src/pages/VideoAnalysis.test.tsx"`：涉及路由跳转的用例统一用 `<Routes>` 包裹，确保点击后能渲染详情页
   - 视需要补充 `"frontend/src/pages/video-analysis/VideoAnalysisHistoryDetail"` 的基础渲染用例
4. 幂等校验
   - 前端：`test` / `lint` / `build`

---

## 2026-06-18 Token 口径统一与汇总展示

### 本次目标
1. “根据解析结果打标”落库 token 使用量（支持多次生成，保留历史）
2. 新增“按视频汇总 token”接口：解析(估算) / 上传视频打标 / 根据解析结果打标 / 总计
3. 前端在详情页展示 token 汇总（避免误解“解析历史 token = 视频累计”）
4. 全部补齐测试，并保证 checks 全绿

### 执行计划
1. 后端：解析结果打标落库 token
   - 修改 `"app/services/analysis_tagging_service.py"`：OpenAI/Anthropic 返回中提取 usage（若存在）并返回给调用方
   - 修改 `"app/api/v1/endpoints/video_analysis.py"`：在 `"video_analyses.result_metadata"` 追加 `analysis_tagging_runs[]`（包含 token_usage）
   - 补测试：`"app/tests/test_analysis_derived_tagging.py"` 验证缓存不重复记账、强制/切换配置会追加记录
2. 后端：视频 token 汇总接口
   - 新增 schema（例如 `"app/schemas/video_analysis.py"`）与接口（例如 `/video-analysis/videos/{video_file_id}/token-summary`）
   - 汇总逻辑：
     - 解析：sum(video_analyses.total_tokens)
     - 上传视频打标：sum(video_auto_tag_tasks.token_usage.*)
     - 解析结果打标：sum(video_analyses.result_metadata.analysis_tagging_runs[].token_usage.*)
   - 补测试：新增 `"app/tests/test_video_token_summary.py"`
3. 前端：展示与文案
   - `"frontend/src/pages/video-analysis/VideoTagsSummary.tsx"`：增加 token 汇总卡片（含分项与总计）
   - `"frontend/src/pages/VideoAnalysis.tsx"`：解析历史 token 文案明确为“本次解析(估算)”
   - 补测试：新增 `"frontend/src/pages/video-analysis/VideoTagsSummary.test.tsx"`，并更新相关断言
4. 幂等校验
   - 后端：pytest
   - 前端：`test` / `lint` / `build`

---

## 本次目标

1. 视频解析选择视频区域增加分页优化
2. 解析历史增加逻辑删除按钮
3. 系统设置中 `"AI API配置"` 页签数字显示修正
4. 标签体系统一为“视频唯一一套结果”

## 执行计划

### 1. 视频选择分页
- 目标：选择视频区域固定每页 `9` 条，按 `3x3` 网格展示
- 现状：当前前端已存在分页，但后端请求尺寸仍是 `12`，前端网格在不同断点下不是固定 `3x3`
- 计划：
  - 调整 `"frontend/src/pages/VideoAnalysis.tsx"` 的视频列表分页大小为 `9`
  - 调整视频卡片网格为桌面端固定 `3` 列
  - 保持现有上一页/下一页交互

### 2. 解析历史逻辑删除
- 目标：增加删除按钮，删除后前端不再展示，但数据库不物理删除
- 现状：`"video_analyses"` 模型已存在 `"is_active"` 字段，可复用为逻辑删除标记
- 计划：
  - 后端新增逻辑删除接口，将 `"is_active"` 置为 `false`
  - 历史列表查询默认只返回 `"is_active = true"` 的记录
  - 前端历史项增加删除按钮，并在删除成功后刷新列表
  - 补充后端/前端相关回归测试

### 3. 系统设置页签数字修正
- 目标：`"AI API配置"` 页签后的数字显示真实配置数
- 现状：页签徽标复用了 `"categories"` 统计，和真实 AI 配置数量不是同一来源
- 计划：
  - 检查 `"frontend/src/pages/SystemConfig.tsx"` 的页签数字来源
  - 对 `"AI API配置"` 单独使用真实 `"configs"` 数量
  - 避免继续受 `"categories"` 统计污染

### 4. 标签体系统一为“视频唯一一套结果”
- 目标：无论来源是自动打标、解析结果打标、人工修订，最终都只归并到视频维度的一套当前生效标签
- 业务规则：
  - 所有入口都只是“产生候选标签 / 写入修订历史”的不同方式
  - 最终展示、后续解析复用、导出、筛选，统一只看该视频当前生效标签集合
  - 历史记录保留来源与版本，但不能再形成“多套并行结果”的产品认知
- 计划：
  - 复核 `"uploaded_file_tags"` / `"uploaded_file_tag_revisions"` 是否已满足“单视频唯一结果集”模型
  - 收敛前端文案，避免出现“解析结果打标是一套、独立打标又是一套”的误解
  - 收敛解析结果打标入口：明确它只是候选生成入口，采纳后仍写入视频统一标签集合
  - 补测试，锁定“不同来源最终归并到同一视频标签集合”的行为

## 验证方式
- 前端：
  - 视频选择页每页显示 `9` 个视频
  - 解析历史可删除且删除后列表隐藏
  - `"AI API配置"` 页签数量与真实配置数一致
  - 不同入口采纳标签后，页面上看到的仍是同一套视频标签结果
- 后端：
  - 逻辑删除后数据库记录仍存在，但 `"is_active = false"`
  - 历史列表接口默认不返回已逻辑删除记录
  - 不同来源写入后，视频当前标签集合唯一且可追溯历史

---

## 2026-06-17 标签体系与自动打标规划

### 当前目标
1. 评估现有“标签体系 / AI 自动打标”设计与实现差距
2. 明确“自动打标”和“基于模板的解析”分离后的架构方案
3. 输出可执行的设计文档与开发计划

### 当前结论
- 自动打标应作为独立能力建设，不与模板解析混成一次输出
- 默认采用“混合模式”：
  - 先读视频做自动打标
  - 模板解析优先复用打标沉淀的结构化结果
  - 仅在需要高细节时才二次读视频
- 标签来源采用：
  - 固定标签库 + 自由标签
- 标签挂载采用：
  - 分析记录保留原始打标结果
  - 视频主记录同步保存当前生效主标签
- 生效策略采用：
  - AI 自动打标先直接生效
  - 支持人工后改
  - 保留版本与变更记录

### 下一步待确认
1. 模板解析复用自动打标时，需要哪些中间产物：
   - 标签
   - 标签置信度
   - 证据时间段
   - 结构化摘要
2. 第一阶段是否先不做复杂的标签关系/冲突规则/统计看板

### 预期输出
- `"docs/superpowers/specs/2026-06-17-auto-tagging-design.md"`
- 后续分阶段开发计划与测试计划

### 分批实施计划
1. 批次 1：数据层打底
   - 新增自动打标相关表与迁移脚本
   - 复用 `tags`，新增 `uploaded_file_tags` 等新表
2. 批次 2：Schema 与 API 骨架
   - 增加自动打标任务、当前有效标签、修订历史接口
3. 批次 3：自动打标服务
   - 打通模型调用、结构化解析、任务落库
4. 批次 4：主标签同步
   - 将有效结果同步到视频当前主标签层
5. 批次 5：人工修订与版本历史
   - 支持 add/remove/adjust 与版本留痕
6. 批次 6：模板解析复用自动打标结果
   - 默认复用标签与摘要，高精度模板才二次读视频
7. 批次 7：前端展示与修订交互
   - 展示自动打标结果、当前主标签、修订历史

### 当前建议优先开工
1. 批次 1：数据层打底
2. 批次 2：Schema 与 API 骨架
3. 批次 3：自动打标服务

---

## 2026-06-17 基于视频解析结果打标规划

### 当前目标
1. 在现有“上传视频自动打标”之外，新增“基于 AI 视频解析结果生成候选标签”的能力
2. 复用现有标签集合、人工修订、历史记录与颜色规则，避免再造一套标签体系
3. 明确该能力与现有“自动打标”的关系、入口、数据来源和落库方式

### 初步方案
- 前端：
  - 在 `"视频打标"` 页面增加第二种来源入口：
    - 基于上传视频自动打标
    - 基于视频解析结果打标
  - 若存在可用解析结果，展示“解析结果候选标签”区块，支持一键加入当前标签
- 后端：
  - 优先复用现有 `"uploaded_file_tags"` 与 `"uploaded_file_tag_revisions"`，不新建第二套主标签表
  - 为视频解析结果补充结构化标签候选输出或解析结果提取逻辑
  - 候选标签采纳后仍通过修订接口进入当前标签集合
- 测试：
  - 前端补充来源切换、候选标签展示、采纳交互测试
  - 后端补充解析结果候选生成/读取测试、修订落库测试

### 待确认口径
1. “基于视频解析结果打标”到底是：
   - 直接从已存在的解析文本/摘要中提取标签
   - 还是重新调用一次 AI，但输入改为“解析结果”而不是视频
2. 候选标签是否需要人工确认后才进入当前标签
3. 解析结果打标是否要保留独立历史来源标识（例如 `"analysis_derived"`）

### 执行草案
1. 明确入口与来源语义
2. 明确候选标签的数据结构与接口
3. 确定是否复用现有修订接口直接落库
4. 输出设计文档
5. 你确认后再进入实现
